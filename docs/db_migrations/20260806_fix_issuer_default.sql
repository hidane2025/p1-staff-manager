-- ============================================================
-- 発行者名義の既定値を現行社名へ修正（2026-08-06）
-- ============================================================
-- 状態: ⚠️ 未適用（2026-08-06作成時点。Supabase Management APIトークンが
--       失効しておりセッションからSQLを実行できないため。
--       適用したらこの2行を「適用済み <日付>」に書き換えること）
--
-- 背景:
--   p1_events.issuer_name の既定値が旧社名「株式会社パシフィック」のまま
--   だった（2026-08-06 空イベント作成プローブで実測）。運営会社は
--   PRT → 株式会社P1 Entertainment へ移行済み（2026-06-27 決定、
--   2026-07-25 領収書名義移行）。既定値のままだと、新規大会の契約書・
--   領収書の甲（発注者）が旧社名で発行される。
--
--   アプリ側は db.create_event が issuer_name を明示するよう修正済み
--   （2026-08-06）のため実運用の穴は塞がっているが、SQLエディタ等から
--   直接行を作った場合に旧社名へ落ちる余地が残る。本マイグレーションで
--   DB側も現行社名に揃える（防御の二重化）。
--
-- 実行: Supabase SQL Editor で本ファイル全体をRun（冪等）
-- ============================================================

ALTER TABLE p1_events
    ALTER COLUMN issuer_name SET DEFAULT '株式会社P1 Entertainment';

-- 甲（発注者）の住所既定値も設定する。登記上の本店所在地
-- （法人番号 1180001113559・商号変更日 2026-06-30・中野さん提供 2026-08-06）
ALTER TABLE p1_events
    ALTER COLUMN issuer_address SET DEFAULT '愛知県名古屋市東区泉1丁目23番37号';

-- 既存イベントの名義は変更しない（過去大会で発行済みの書類の名義を保全）。
-- 8月大阪の2イベント(id=10,11)は「株式会社P1 Entertainment」設定済みで影響なし。

-- 確認用:
--   SELECT column_default FROM information_schema.columns
--    WHERE table_name = 'p1_events' AND column_name = 'issuer_name';
