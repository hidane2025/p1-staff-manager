-- ============================================================
-- Event Prefecture Migration (2026-05-04)
-- イベント会場の都道府県カラム追加
-- ============================================================
-- 目的: 開催地（venue）に加えて都道府県を独立して保持。
--       地域別交通費ルール（p1_event_transport_rules.is_venue_region）の
--       自動判定基準として、また年次集計の地域分析に利用する。
-- ============================================================
-- 実行: Supabase SQL Editor で本ファイル全体を貼り付けて Run
-- ============================================================

-- 1. p1_events に開催地都道府県カラムを追加
--    後方互換: NULL 許容。既存データは NULL のままでアプリ動作可能。
ALTER TABLE p1_events
    ADD COLUMN IF NOT EXISTS prefecture TEXT;

-- 2. p1_events に休憩ルール表示メモ用のテンプレ識別を追加（オプション）
--    どのレートプリセットから派生したかを記録。空文字は手入力扱い。
ALTER TABLE p1_events
    ADD COLUMN IF NOT EXISTS rate_template_id TEXT DEFAULT '';

-- 3. 参考: アプリ側は utils/db_schema.has_column() 経由で参照するため、
--    マイグレ未実行でも従来どおり動作する（カラムが無いものとして扱う）。
