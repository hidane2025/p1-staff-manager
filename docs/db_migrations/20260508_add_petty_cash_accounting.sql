-- ============================================================
-- Phase 2 #2-2: 小口経費の勘定科目・宛名カラム追加（2026-05-08）
-- ============================================================
-- 背景:
--   現場フィードバック（伊藤さん経由）：
--   小口経費を「社内運用資料としての利用目的」にしたく、
--   勘定科目の記載箇所と領収書宛名の記載箇所を追加したい。
--   AI回答: 法律上は不要だが、税務上は必要。
--
-- 実行: Supabase SQL Editor で本ファイル全体を貼り付けて Run
-- ============================================================

-- 1. 勘定科目カラム（任意）
ALTER TABLE p1_petty_cash
    ADD COLUMN IF NOT EXISTS account_code TEXT;

-- 2. 領収書宛名カラム（任意）
ALTER TABLE p1_petty_cash
    ADD COLUMN IF NOT EXISTS payee_name TEXT;

-- 3. 参考: アプリ側は utils/db_schema.has_column() 経由で参照するため、
--    マイグレ未実行でも従来どおり動作する（カラムが無いものとして扱う）。
--
-- 一般的な勘定科目例（参考・固定値ではなく自由入力）:
--   旅費交通費 / 通信費 / 消耗品費 / 接待交際費 / 会議費
--   雑費 / 修繕費 / 福利厚生費 / 業務委託費 / 広告宣伝費
